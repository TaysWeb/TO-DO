let task = [];

  // Use of Date.now() method
  var date = Date(Date.now());
 
  //Converting the number of a millisecond in the date string

  let createdDate = date.toString();
  console.log(currentDate)

function addPerson (){
    let tasks = {
        id: task.indexOf() ,
        name: document.getElementById('insert'),
        
    }
    task.push(tasks);
    console.log(task)

}
